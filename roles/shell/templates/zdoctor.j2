#!/bin/bash
# zdoctor - Health check for Zen terminal environment
# Generated by Zen setup

ZEN_DIR="{{ zen_setup_dir }}"

if [[ -f "$ZEN_DIR/bin/zdoctor" ]]; then
  exec bash "$ZEN_DIR/bin/zdoctor" "$@"
else
  echo "Error: zdoctor not found at $ZEN_DIR/bin/zdoctor"
  exit 1
fi
