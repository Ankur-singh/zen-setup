#!/bin/bash
# zsetup - Run Zen setup from anywhere
# Generated by Zen setup

ZEN_DIR="{{ zen_setup_dir }}"

if [[ ! -d "$ZEN_DIR" ]]; then
  echo "Error: Zen directory not found at $ZEN_DIR"
  echo "Please reinstall Zen or set ZEN_SETUP_DIR"
  exit 1
fi

exec bash "$ZEN_DIR/bootstrap.sh" "$@"
